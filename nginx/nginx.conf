# DEV
server {
    listen 80;
    server_name dev.localhost;
    resolver 127.0.0.11 valid=30s;

    location /api {
        set $upstream http://backend-dev:5000;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location ~* \.(js|css|woff2|woff|ttf|png|jpg|svg|ico)$ {
        set $upstream http://frontend-dev:4173;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        add_header Cache-Control "public, max-age=31536000, immutable" always;
    }

    location / {
        set $upstream http://frontend-dev:4173;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_hide_header Cache-Control;
        proxy_hide_header ETag;
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
        add_header Pragma "no-cache" always;
        add_header Expires "0" always;
    }
}

# TEST
server {
    listen 80;
    server_name test.localhost;
    resolver 127.0.0.11 valid=30s;

    location /api {
        set $upstream http://backend-test:5001;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location ~* \.(js|css|woff2|woff|ttf|png|jpg|svg|ico)$ {
        set $upstream http://frontend-test:4173;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        add_header Cache-Control "public, max-age=31536000, immutable" always;
    }

    location / {
        set $upstream http://frontend-test:4173;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_hide_header Cache-Control;
        proxy_hide_header ETag;
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
        add_header Pragma "no-cache" always;
        add_header Expires "0" always;
    }
}

# STAGING
server {
    listen 80;
    server_name staging.localhost;
    resolver 127.0.0.11 valid=30s;

    location /api {
        set $upstream http://backend-staging:5002;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location ~* \.(js|css|woff2|woff|ttf|png|jpg|svg|ico)$ {
        set $upstream http://frontend-staging:4173;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        add_header Cache-Control "public, max-age=31536000, immutable" always;
    }

    location / {
        set $upstream http://frontend-staging:4173;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_hide_header Cache-Control;
        proxy_hide_header ETag;
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
        add_header Pragma "no-cache" always;
        add_header Expires "0" always;
    }
}

# PROD
server {
    listen 80;
    server_name prod.localhost;
    resolver 127.0.0.11 valid=30s;

    location /api {
        set $upstream http://backend-prod:5003;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location ~* \.(js|css|woff2|woff|ttf|png|jpg|svg|ico)$ {
        set $upstream http://frontend-prod:4173;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        add_header Cache-Control "public, max-age=31536000, immutable" always;
    }

    location / {
        set $upstream http://frontend-prod:4173;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_hide_header Cache-Control;
        proxy_hide_header ETag;
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
        add_header Pragma "no-cache" always;
        add_header Expires "0" always;
    }
}